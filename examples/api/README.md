# API examples

These `.http` files work with JetBrains HTTP Client and the VS Code REST Client extension. Start the services, run catalog requests to obtain the current train-run and seat IDs, create a fare quote, then copy response IDs into `booking-flow.http`.

All identities and passenger details are demonstration values. Never paste production credentials or passenger information into committed examples.
